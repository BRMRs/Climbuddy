- Migrated all local modal wrappers in PartnersTab, GymDetailScreen, and ProgressTab to use shared Modal component.
- Verified build passes with vite, and committed changes as fix(modals): migrate ReviewOverlay in PartnersTab to shared Modal.
- Next steps: perform codebase-wide search for any remaining local modal wrappers and update as necessary.
- Added review creation flow directly inside CalendarSection for expired booking events using shared Modal and 4-dimension star inputs.
- Review submission pattern: write review into lifted App state (`venueReviews`) and separately mutate event state (`calendarEvents`) to mark `isReviewed`.
